import React from 'react'
import ReactDOM from 'react-dom/client'
import BriefingAgent from './components/BriefingAgent.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BriefingAgent />
  </React.StrictMode>,
)
